{
  "name": "switchon-diet-companion",
  "version": "1.0.0",
  "private": true,
  "description": "Switch-On diet companion web app with a Capacitor iPhone app wrapper",
  "scripts": {
    "serve": "npx http-server . -p 4173 -c-1",
    "cap:sync": "npx cap sync",
    "cap:ios:add": "npx cap add ios",
    "cap:ios:open": "npx cap open ios",
    "cap:ios:run": "npx cap run ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "http-server": "^14.1.1"
  }
}
